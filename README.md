## GNN4CD-CORDEXML - branch Dev_downscaler

This folder contains the tests for improving:
- The `encoder` module, which learns Low nodes embedding from the atmospheric information
- The `downscaler` module, which learns High nodes embedding starting from the Low nodes embedding and the static high-res data